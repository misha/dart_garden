# Garden Dogs

This package provides a single `GardenPlugin` compatible with [`dogs`](https://pub.dev/packages/dogs), a powerful serialization framework. By using the plugin, you can transparently serialize and deserialize `garden` leaves without additional code generation.
